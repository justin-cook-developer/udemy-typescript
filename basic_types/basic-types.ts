// numbers
function add(a: number, b: number): number {
  return a + b;
}

const onePlusTwo: number = add(1, 2);

// string
const myName: string = "Justin";

// boolean
const is21: boolean = true;

console.log(myName, is21);
